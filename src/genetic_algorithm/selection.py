# Written by Ashlyn ❀⸜(˶´ ˘ `˶)⸝❀

def tournament_selection(population, results):
    # survival of the fittest logic (in random subsets to vary what passes on)

def select_elites(population, results):
    # pick the best individuals of the gen and ensure they carry into the next

def selection_process(population, results, selection_percentage):
    select_elites(population, results)
